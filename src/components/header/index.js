import UpperNav from "../upper-nav";
import Slider from "../slider";


const Header = () => {
    return(
        <div>
            <UpperNav />
            <Slider />
        </div>
    );
};

export default Header;